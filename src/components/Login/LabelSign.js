import React from 'react'
import classes from './LabelSign.module.css'

const LabelSign = () => {
  return (
    <div>
        <div className={classes.signIn}>Sign in with:</div>
    </div>
  )
}

export default LabelSign
